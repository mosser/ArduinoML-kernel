application redButton

# Declaring bricks
brickss:
    sensor button: 9
    actuator led: 12

# Declaring states
states:

    -> on {
        led <= HIGH
        button is HIGH => off
    }

    off {
        led <= LOW
        button is HIGH => on
    }
